# Fazer um programa para ler uma duração de tempo em segundos,
# daí imprimir na tela esta duração no formato horas:minutos:segundos.


st = float(input("Digite a duracao em segundos: "))

horas = st//3600
minutos = (st%3600)//60
segundos = (st%3600)%60

print(f"{horas:.0f}:{minutos:.0f}:{segundos:.0f}")
